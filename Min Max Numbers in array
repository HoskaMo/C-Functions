int MinNumber(int arr[100], int arrayLength)
{
    int min = 0;
    min = arr[0];
    for (int i = 0; i < arrayLength; i++)  
    {
        if (arr[i] < min)
        {
            min = arr[i];
        }
    }
    return min;
}
